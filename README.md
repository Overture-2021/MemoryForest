
  # Memory Forest

  This is a code bundle for Memory Forest. The original project is available at https://www.figma.com/design/XoJBf42IKRAU2G2noJdzr6/Memory-Forest.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  