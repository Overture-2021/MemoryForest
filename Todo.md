Make selection stronger than hover. Right now hover highlighting is nice, but pinned highlights for a selected person/thread/event would help users study patterns instead of just inspect momentarily.

Add import/export. 

Quick timeline navigation